import { Shader } from "react-shaders";

// iTime iReso

export const ShaderBackground = () => {
  return (
    <div className="w-screen h-screen fixed">
      {/* <Shader fs={shader} /> */}
    </div>
  );
};
